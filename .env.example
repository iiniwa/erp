# Copy this file to .env at the repo root and adjust as needed for local development.
# `make up` / `docker compose` picks this up automatically via --env-file .env.

# --- MariaDB ---
DB_ROOT_PASSWORD=root
DB_NAME=app_development
DB_USERNAME=app
DB_PASSWORD=app
DB_PORT=3306

# --- Rails backend ---
BACKEND_PORT=3001
# Generate locally with: bin/rails secret (or fetch from your team's secret store)
RAILS_MASTER_KEY=

# --- Next.js frontend ---
FRONTEND_PORT=3000

# --- Internal BFF -> Rails API authentication (shared secret) ---
INTERNAL_API_SECRET=dev-internal-secret

# --- SFTPGo ---
SFTPGO_ADMIN_USER=admin
SFTPGO_ADMIN_PASSWORD=admin12345
SFTPGO_WEBADMIN_PORT=8080
SFTPGO_SFTP_PORT=2022
